{
  "name": "typescript-dep",
  "version": "0.0.0",
  "scripts": {
    "prebuild": "rm -rf dist/*",
    "build": "tsc",
    "dev": "tsc -w",
    "test": "jest test",
    "test:watch": "jest --watch"
  },
  "devDependencies": {
    "jest": "^27.3.1",
    "typescript": "^4.3.2"
  }
}
